import React from "react";

//Criando o contexto da aplicação (contexto = "state" global, pode ser acessado em todos filhos sem passar por prop).
const AppContext = React.createContext();

export default AppContext;
